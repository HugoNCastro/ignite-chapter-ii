{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node", // flag responsável por passar a linguagem que estamos usando
      "request": "attach", 
				//flag que serve para dizer em qual momento esperamos que o debugger seja acionado	
				//com o valor attach será disparado o erro após receber a requisição
      "name": "Launch Program",
      "skipFiles": [
        "<node_internals>/**"
      ],
      "outFiles": [
        "${workspaceFolder}/**/*.js"
      ]
    }
  ]
}